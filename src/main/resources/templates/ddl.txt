-- public.sms_info definition

-- Drop table

-- DROP TABLE sms_info;

CREATE TABLE sms_info (
	objectid int4 NOT NULL GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE),
	booking_date timestamp NULL,
	booth_addr varchar(255) NULL,
	booth_id varchar(255) NULL,
	created_dt timestamp NULL,
	globalid varchar(255) NULL,
	hoh_name varchar(255) NULL,
	is_otp_verified bool NULL,
	received_otp varchar(255) NULL,
	receiver varchar(255) NULL,
	rel_globalid varchar(255) NULL,
	relation_manager_no varchar(255) NULL,
	sent_otp varchar(255) NULL,
	sms_type varchar(255) NULL,
	survey_id varchar(255) NULL,
	time_slot varchar(255) NULL,
	transaction_id varchar(255) NULL,
	updated_dt timestamp NULL,
	CONSTRAINT sms_info_pkey PRIMARY KEY (objectid)
);

-- Permissions

ALTER TABLE public.sms_info OWNER TO postgres;
GRANT ALL ON TABLE public.sms_info TO postgres;